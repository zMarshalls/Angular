// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCy9ExKkmtz1nvO_e943omF-eH2KV8dcVo",
    authDomain: "meu-projeto-incrivel-2b70c.firebaseapp.com",
    databaseURL: "https://meu-projeto-incrivel-2b70c.firebaseio.com",
    projectId: "meu-projeto-incrivel-2b70c",
    storageBucket: "meu-projeto-incrivel-2b70c.appspot.com",
    messagingSenderId: "619470362483"
  }
};

/*
 * In development mode, for easier debugging, you can ignore zone related error
 * stack frames such as `zone.run`/`zoneDelegate.invokeTask` by importing the
 * below file. Don't forget to comment it out in production mode
 * because it will have a performance impact when errors are thrown
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
